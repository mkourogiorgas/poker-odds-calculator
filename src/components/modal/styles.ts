const description = 'mt-2 text-sm text-gray-600';

const image = 'mx-auto w-16 h-16';

const modalBox =
  'bg-white p-6 rounded-lg shadow-lg w-96 text-center';

const overlay =
  'fixed bg-black/60 inset-0 z-50 flex items-center justify-center focus:outline-none';

const tip = 'mt-2 text-xs text-gray-500';

const title = 'mt-4 text-gray-800';

export {
  description,
  image,
  modalBox,
  overlay,
  tip,
  title,
};
