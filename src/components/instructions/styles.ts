const container =
  'mt-3 bg-white rounded-lg p-3 shadow-md hover:shadow-lg transition-shadow';

const iconBase = 'text-base';

const row = 'flex items-center gap-2';

const text = 'text-xs text-gray-700';

export { container, iconBase, row, text };
